# Database and transactions

Drizzle RC uses the Effect PostgreSQL driver. All repositories resolve the private transaction context before falling back to Database. Nested TransactionService.run calls join the outer transaction. Expected domain failures remain domain failures; only SQL/Drizzle errors become DatabaseError.

Gift transitions, job enqueueing, and audit rows share a transaction. Workflow RPC/provider calls execute outside database transactions. The transaction journal deliberately holds a database lock while preparing and signing an operation, then broadcasts only after the encrypted operation commits. Jobs use PostgreSQL row locks with SKIP LOCKED, expiring leases, and a unique deduplication key. Completion uses the lease token to reject stale workers.

Identifiers shared with contracts are 32-byte hex values. Application timestamps are Unix milliseconds; policy and session deadlines are Unix seconds. Amounts are decimal strings at JSON boundaries and exact PostgreSQL numerics where arithmetic is needed.

Migrations are generated by Drizzle Kit and reviewed before committing. PGlite tests apply the checked-in migrations. `pnpm test:postgres` verifies concurrent migration startup, independent job leases, stale-worker fencing and serialized coordinator nonces using a disposable PostgreSQL 17 container. PGlite does not model multiple connections.

Migrations acquire a transaction-scoped PostgreSQL advisory lock before reading migration history. Successful job progress resets the consecutive-failure budget; transient failures back off and eventually require explicit retry. The complete worker update is fenced by the lease token, including claim and gift transitions.

Each worker operation has a 90-second deadline, shorter than its 120-second lease. A stalled provider becomes a retryable job error; transaction retries reconcile the recorded operation.

## ENSForge storage

`ens_workflows` stores opaque encrypted SDK values keyed by namespace and ID, with a revision column.
The repository supports create-if-absent, read, ordered pagination and a single-statement compare-and-swap
that requires exactly one revision increment. It joins TransactionService like other repositories.
The server scopes SDK namespaces by claim and authenticates claim/namespace/ID/revision in encryption
associated data. SDK secrets never become public claim fields. PGlite tests cover isolation, restoration
and revision conflicts; the PostgreSQL suite races independent creates and updates.
