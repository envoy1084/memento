# @memento/protocol

Shared runtime schemas, public DTOs, identities, states, and typed errors. No infrastructure dependencies.

Run `pnpm --filter @memento/protocol typecheck`, `test`, or `build` from the repository root.

Explicit `Preview*` schemas describe the browser-only interactive demo. They are separate from live API contracts and contain no credentials.
