# @memento/database

Drizzle schema, repositories, migrations, and transaction-aware Effect services. Tests use migrated PGlite.

Run `pnpm --filter @memento/database typecheck`, `test`, or `build` from the repository root.
