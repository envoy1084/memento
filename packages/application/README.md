# @memento/application

Effect workflows for email-bound new-name gifts, funding confirmation, recipient claims, refunds and durable email/claim jobs. Delivery contacts and registration secrets are encrypted. Funding confirmation schedules delivery atomically with the ready state.

Run `pnpm --filter @memento/application typecheck`, `test`, or `build` from the repository root.
