# @memento/application

Business workflows composed from injectable Effect services. HTTP and background jobs use the same Application service.

Run `pnpm --filter @memento/application typecheck`, `test`, or `build` from the repository root.
