{
  "name": "@memento/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build && node ../chain/scripts/generate-abis.mjs --check",
    "test": "forge test",
    "lint": "forge fmt --check"
  },
  "dependencies": {
    "@openzeppelin/contracts": "catalog:"
  },
  "devDependencies": {
    "klarity": "catalog:"
  }
}
